const mongoose = require('mongoose');

// async function confirmEmail(confirmed) {
//     try {
//       const user = await User.findOne({ confirmed });
//       return user;
//     } catch (error) {
//       console.error('Error finding user:', error);
//       throw error;
//     }
// };

document.addEventListener("DOMContentLoaded", () => {
    
});